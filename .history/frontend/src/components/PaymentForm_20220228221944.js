import React from 'react'
import {Container, Row, Form, FormGroup, FormControl, Panel} from 'react-bootstrap'

const PaymentForm  = () =>{
    return(
        <Container>
        
        </Container>
    );
}

export default PaymentForm;